# Third-party notices

This package uses `chmlib-ts` as a runtime dependency. `chmlib-ts` is a
TypeScript port of CHMLib and contains LZX decompression code derived from
cabextract. It is distributed under LGPL-2.1. Its complete license text is
included in the installed `chmlib-ts` package as `COPYING`.

This package also uses DOMPurify, distributed under the Apache-2.0 or MPL-2.0
license.
